def func():
    print "F(x) = 2x + 3"
    x = int(raw_input('Enter an integer value for x: '))
    Fx = 2 * x + 3
  if in range (0,27) 
  print "Nooo!"
  if in range (x > 27)
  print "Yay!"
    return Fx

print func()
